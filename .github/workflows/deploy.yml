name: Build and Deploy HR Import Service

# Grant explicit permissions so the workflow can push packages when using GITHUB_TOKEN
permissions:
  contents: read
  packages: write

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    
    - name: Build with Maven
      run: mvn -B package -DskipTests

    - name: Show target directory (debug)
      run: |
        echo "Listing target/ contents:" 
        ls -la target || true

    - name: Ensure jar exists
      run: |
        if ls target/*.jar 1> /dev/null 2>&1; then
          echo "JAR found:"; ls -la target/*.jar
        else
          echo "ERROR: No jar produced in target/"; ls -la || true; exit 1
        fi

    - name: Run Tests
      run: mvn test
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: hr-import-service
        path: target/*.jar

  run-image:
    needs: build
    uses: ./.github/workflows/run-docker-on-github.yml
    with:
      image: hr-import-service:latest
      build: true
      context: .
      dockerfile: Dockerfile
      ports: ""
      env: ""
      run_args: ""
      run_cmd: ""
    secrets:
      docker_registry_username: ${{ secrets.DOCKER_USERNAME }}
      docker_registry_password: ${{ secrets.DOCKER_PASSWORD }}

  publish-image:
    name: Build and Publish Image to GHCR
    needs: [build, run-image]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Download built JAR artifact
        uses: actions/download-artifact@v4
        with:
          name: hr-import-service
          path: artifact

      - name: Prepare JAR for Docker build
        run: |
          echo "Copying jar into repo workspace for Docker build"
          cp artifact/*.jar ./app.jar
          ls -la ./app.jar || true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry (GHCR)
        run: |
          # Prefer a Personal Access Token stored in GHCR_PAT when organization policies restrict package creation.
          if [ -n "${{ secrets.GHCR_PAT }}" ]; then
            echo "Using GHCR_PAT (personal access token) to login as ${{ github.actor }}"
            echo "${{ secrets.GHCR_PAT }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          elif [ -n "${{ secrets.GITHUB_TOKEN }}" ]; then
            echo "GHCR_PAT not provided; attempting login with GITHUB_TOKEN as ${{ github.actor }}"
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin || (
              echo "Login with GITHUB_TOKEN failed. If your organization restricts package creation, create a PAT with 'write:packages' and add it to repository secrets as GHCR_PAT."; exit 1
            )
          else
            echo "No GHCR_PAT or GITHUB_TOKEN available. Create a PAT with 'write:packages' and add it as repository secret GHCR_PAT."; exit 1
          fi

      - name: Build and push image to GHCR
        env:
          IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/hr-import-service
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "Building Docker image ${IMAGE_NAME}:${IMAGE_TAG}"
          docker build -t ${IMAGE_NAME}:${IMAGE_TAG} .
          echo "Pushing Docker image ${IMAGE_NAME}:${IMAGE_TAG}"
          docker push ${IMAGE_NAME}:${IMAGE_TAG}

      - name: Confirm pushed image
        run: |
          echo "Pushed image: ghcr.io/${{ github.repository_owner }}/hr-import-service:${{ github.sha }}"
